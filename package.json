{
	"name": "obsidian-meeting-recorder",
	"version": "0.1.0",
	"description": "Record meetings in Obsidian, transcribe locally with Whisper, and summarize with Claude.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "node esbuild.config.mjs production"
	},
	"keywords": ["obsidian-plugin", "whisper", "transcription", "meeting"],
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.0.0",
		"esbuild": "^0.21.0",
		"obsidian": "latest",
		"typescript": "^5.4.0"
	}
}
